# PyTorch is intentionally not pinned here because the right build depends on
# your machine, CUDA version, and whether you want CPU-only or GPU support.
# Install a machine-appropriate torch build first, then install this file.
# Example flow:
#   pip install torch
#   pip install -r experiments/requirements.txt

Pillow>=10.0.0
accelerate
diffusers
transformers
rp
easydict
einops
icecream
imageio
opencv-python
pandas
scipy
tensorboard
tensorboardX
tqdm
py3nvml
